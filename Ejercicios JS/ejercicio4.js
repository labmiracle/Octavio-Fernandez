/*
function palabraMasLarga(frase) {
  let word = frase.split(' ');
  let new_word = '';

  for (i = 0; i < word.length; i++) {
    if (new_word.length < word[i].length) {
      new_word = word[i];
    }
  }

  return console.log(new_word);
  palabraMasLarga("Erase una vez que se era");
}


function primeraMayuscula(palabras){
   let words = palabras.split(' ');
   let firstLetter = [];

   for (let index = 0; index < words.length; index++) {
    const word = words[index];
      firstLetter.push(word[0].toUpperCase()+word.slice([1,]))
   }

   console.log(firstLetter)
}

primeraMayuscula("chavo chavito que andas haciendo manito")

*/